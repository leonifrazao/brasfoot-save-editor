---
phase: 260405-pv4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/application.properties
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionController.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java
autonomous: true
requirements:
  - 260405-pv4

must_haves:
  truths:
    - Application properties set multipart max file size and max request size to 500MB
    - POST /api/v1/sessions rejects files that do not have the .s22 extension
    - POST /api/v1/sessions returns RFC-7807 ProblemDetail 400 Bad Request on invalid extension
  artifacts:
    - path: src/main/resources/application.properties
      provides: Multipart config
    - path: src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionController.java
      provides: Extension validation
  key_links:
    - from: src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionController.java
      to: MultipartFile
      via: getOriginalFilename().endsWith(".s22")
---

<objective>
Increase multipart upload limits to 500MB and add .s22 file format validation.

Purpose: Allow larger save files (e.g., .s22 up to 500MB) while rejecting unsupported file types.
Output: Updated properties, updated controller with validation, updated global exception handler for size limits.
</objective>

<context>
@src/main/resources/application.properties
@src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionController.java
@src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase Multipart Upload Limits</name>
  <files>src/main/resources/application.properties, src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java</files>
  <action>
    Add `spring.servlet.multipart.max-file-size=500MB` and `spring.servlet.multipart.max-request-size=500MB` to application.properties.
    Add an `@ExceptionHandler` for `org.springframework.web.multipart.MaxUploadSizeExceededException` in `GlobalExceptionHandler` to return a 400 or 413 ProblemDetail with a clear message (e.g., "File size exceeds the 500MB limit.").
  </action>
  <verify>
    <automated>grep "500MB" src/main/resources/application.properties</automated>
  </verify>
  <done>Properties contain the 500MB limits and GlobalExceptionHandler handles the upload size exception.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate .s22 File Extension</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionController.java, src/test/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionControllerTest.java</files>
  <behavior>
    - Test 1: Uploading a file with ".s22" extension succeeds
    - Test 2: Uploading a file with ".txt" extension fails with 400 Bad Request
  </behavior>
  <action>
    Update `POST /api/v1/sessions` in `SessionController`.
    Check if `file.getOriginalFilename()` is null or does not end with ".s22" (ignore case if possible, but strict ".s22" is fine).
    If it fails validation, throw an `IllegalArgumentException` with the message "Invalid file format. Only .s22 files are supported." (this is automatically mapped to 400 ProblemDetail by `GlobalExceptionHandler`).
    Update Swagger `@Parameter` description to indicate `.s22` instead of `.sav`.
    Run/write test to verify extension validation.
  </action>
  <verify>
    <automated>mvn test -Dtest=SessionControllerTest</automated>
  </verify>
  <done>SessionController rejects non-.s22 files and throws IllegalArgumentException.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted file upload crossing the HTTP boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260405-pv4-01 | Tampering | SessionController | mitigate | Enforce .s22 extension check on `file.getOriginalFilename()`. |
| T-260405-pv4-02 | Denial of Service | Multipart Config | mitigate | Enforce strict 500MB max file size to prevent OOM / unbounded disk usage. |
</threat_model>

<verification>
File sizes and file formats are correctly constrained per user request.
</verification>

<success_criteria>
- application.properties limits are set to 500MB.
- SessionController rejects any file that does not end in .s22 with a 400 problem detail.
</success_criteria>

<output>
After completion, create `.planning/phases/260405-pv4-increase-the-multipart-upload-size-limit/260405-pv4-01-SUMMARY.md`
</output>
