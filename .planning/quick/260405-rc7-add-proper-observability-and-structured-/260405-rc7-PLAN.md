---
phase: quick
plan: 260405-rc7
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/resources/logback-spring.xml
  - src/main/resources/application.properties
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/LoggingFilter.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java
  - src/main/java/br/com/saveeditor/brasfoot/application/services/SessionService.java
  - src/main/java/br/com/saveeditor/brasfoot/application/services/PlayerManagementService.java
  - src/main/java/br/com/saveeditor/brasfoot/application/services/TeamManagementService.java
autonomous: true
requirements: []
must_haves:
  truths:
    - "Application logs are output in JSON format via LogstashEncoder"
    - "All HTTP requests and responses are logged with duration in ms"
    - "Unhandled exceptions are logged with full stack traces at ERROR level"
    - "Normal use case flows log at INFO, while detailed state changes log at DEBUG"
  artifacts:
    - path: "src/main/resources/logback-spring.xml"
      provides: "SLF4J/Logback JSON configuration"
    - path: "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/LoggingFilter.java"
      provides: "Spring Web filter for request/response logging"
  key_links:
    - from: "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/LoggingFilter.java"
      to: "org.springframework.web.filter.OncePerRequestFilter"
      via: "inheritance"
      pattern: "extends OncePerRequestFilter"
---

<objective>
Add proper observability and structured JSON logging to the application using SLF4J and Logback.

Purpose: Improve debuggability, auditing, and error tracking across all layers by providing consistent JSON-structured logs, request tracking, full stack traces for unhandled exceptions, and domain-level events.
Output: Updated `pom.xml`, new `logback-spring.xml` config, `LoggingFilter` for HTTP, enhanced `GlobalExceptionHandler`, and service-level logging.
</objective>

<context>
@.planning/quick/260405-rc7-add-proper-observability-and-structured-/260405-rc7-CONTEXT.md
@src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Logstash Encoder and Logback Config</name>
  <files>pom.xml, src/main/resources/logback-spring.xml, src/main/resources/application.properties</files>
  <action>Add the `logstash-logback-encoder` dependency to `pom.xml`. Create `src/main/resources/logback-spring.xml` configuring `ConsoleAppender` with `net.logstash.logback.encoder.LogstashEncoder` to output JSON. Add `logging.level.br.com.saveeditor.brasfoot=DEBUG` to `application.properties` to ensure our domain DEBUG logs are visible.</action>
  <verify>
    <automated>mvn clean compile</automated>
  </verify>
  <done>JSON logging dependency added and configuration file created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Request/Response Logging & Exception Stack Traces</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/LoggingFilter.java, src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java</files>
  <action>Create a Spring `@Component` named `LoggingFilter` extending `OncePerRequestFilter`. Override `doFilterInternal` to capture the start time, invoke `filterChain.doFilter`, and in a `finally` block log `[HTTP {} {} - Status: {} - {}ms]` at INFO level using SLF4J. Update `GlobalExceptionHandler` by adding a `Logger` instance. Log domain exceptions (400, 404, 410, 413) at WARN level, and log `Exception.class` catch-all at ERROR level passing the exception object to output the full stack trace.</action>
  <verify>
    <automated>mvn clean compile</automated>
  </verify>
  <done>HTTP filter implemented and exceptions properly logged.</done>
</task>

<task type="auto">
  <name>Task 3: Add Domain-Level Logging</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/application/services/SessionService.java, src/main/java/br/com/saveeditor/brasfoot/application/services/PlayerManagementService.java, src/main/java/br/com/saveeditor/brasfoot/application/services/TeamManagementService.java</files>
  <action>Instantiate an SLF4J `Logger` in all three services. In `SessionService`, log INFO when a session is created/deleted/downloaded, and DEBUG for I/O details. In `PlayerManagementService` and `TeamManagementService`, log INFO when entities are updated, and DEBUG to output the specifics (e.g., player stat changes or team updates).</action>
  <verify>
    <automated>mvn clean compile</automated>
  </verify>
  <done>Domain layer logs lifecycle events at INFO and state details at DEBUG.</done>
</task>

</tasks>

<success_criteria>
- Application emits JSON-formatted logs to standard output.
- All incoming HTTP requests generate a structured log line including method, path, status, and duration.
- Expected domain exceptions (like SessionNotFound) log at WARN without crashing.
- Unexpected exceptions log a full stack trace at ERROR level.
- Core use cases log their progress using INFO and DEBUG levels.
</success_criteria>

<output>
After completion, verify JSON log structure during server boot-up.
</output>
