# Quick Task 260405-q11: after the session file is downloaded via GET /api/v1/sessions/{id}/download, automatically delete the session from cache. also add edge case handling: session not found (404), expired session (410 Gone), and attempting to download an already-deleted session (404).

**Gathered:** 2026-04-05
**Status:** Ready for planning

<domain>
## Task Boundary

after the session file is downloaded via GET /api/v1/sessions/{id}/download, automatically delete the session from cache. also add edge case handling: session not found (404), expired session (410 Gone), and attempting to download an already-deleted session (404).
</domain>

<decisions>
## Implementation Decisions

### Cache Eviction and Tombstones
- Soft Delete (Tombstone): Insert a small 'tombstone' object indicating removal time/reason instead of true eviction to differentiate between 'never existed', 'expired', and 'deleted'.

### HTTP Status Mapping
- Strict 404/410: 404 for never existed, 410 for expired, 404 for explicitly deleted sessions.
</decisions>

<specifics>
## Specific Ideas

No specific requirements — open to standard approaches
</specifics>

<canonical_refs>
## Canonical References

No external specs — requirements fully captured in decisions above
</canonical_refs>