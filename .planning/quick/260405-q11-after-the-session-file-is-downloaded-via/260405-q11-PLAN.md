---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/br/com/saveeditor/brasfoot/domain/exceptions/SessionNotFoundException.java
  - src/main/java/br/com/saveeditor/brasfoot/domain/exceptions/SessionExpiredException.java
  - src/main/java/br/com/saveeditor/brasfoot/domain/exceptions/SessionDeletedException.java
  - src/main/java/br/com/saveeditor/brasfoot/application/ports/out/SessionStatePort.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/out/cache/CaffeineSessionAdapter.java
  - src/main/java/br/com/saveeditor/brasfoot/application/services/SessionService.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java
  - src/test/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionControllerTest.java
autonomous: true
requirements: []
must_haves:
  truths:
    - Downloading a session returns the file and deletes the session
    - Downloading a never-existed session returns 404
    - Downloading an expired session returns 410 Gone
    - Downloading an already-deleted session returns 404 Not Found
  artifacts:
    - path: src/main/java/br/com/saveeditor/brasfoot/domain/exceptions/SessionExpiredException.java
      provides: Exception for 410
    - path: src/main/java/br/com/saveeditor/brasfoot/domain/exceptions/SessionDeletedException.java
      provides: Exception for 404
    - path: src/main/java/br/com/saveeditor/brasfoot/adapters/out/cache/CaffeineSessionAdapter.java
      provides: Tombstone cache
    - path: src/main/java/br/com/saveeditor/brasfoot/application/services/SessionService.java
      provides: Post-download deletion logic
  key_links:
    - from: src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java
      to: Exception classes
      pattern: handleSessionExpiredException.*410
---

<objective>
Implement automatic session deletion after download and cache tombstoning for proper 404/410 HTTP responses on missing sessions.

Purpose: Free memory after session consumption and clearly differentiate between a session that expired vs one that was already downloaded.
Output: Domain exceptions, updated caching adapter with tombstones, and GlobalExceptionHandler updates.
</objective>

<context>
@src/main/java/br/com/saveeditor/brasfoot/adapters/out/cache/CaffeineSessionAdapter.java
@src/main/java/br/com/saveeditor/brasfoot/application/services/SessionService.java
@src/main/java/br/com/saveeditor/brasfoot/application/ports/out/SessionStatePort.java
</context>

<tasks>

<task type="auto">
  <name>Create Domain Exceptions</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/domain/exceptions/SessionNotFoundException.java, src/main/java/br/com/saveeditor/brasfoot/domain/exceptions/SessionExpiredException.java, src/main/java/br/com/saveeditor/brasfoot/domain/exceptions/SessionDeletedException.java</files>
  <action>
    Create three `RuntimeException` subclasses in the `domain.exceptions` package:
    - `SessionNotFoundException`: Used when a session UUID does not exist at all.
    - `SessionExpiredException`: Used when a session expired from the cache over time.
    - `SessionDeletedException`: Used when a session was explicitly removed (downloaded).
  </action>
  <verify>
    <automated>ls src/main/java/br/com/saveeditor/brasfoot/domain/exceptions/SessionNotFoundException.java</automated>
  </verify>
  <done>Exceptions extend RuntimeException and take a message in constructor.</done>
</task>

<task type="auto">
  <name>Implement Tombstone Caching</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/application/ports/out/SessionStatePort.java, src/main/java/br/com/saveeditor/brasfoot/adapters/out/cache/CaffeineSessionAdapter.java</files>
  <action>
    Modify `SessionStatePort` so `load(UUID id)` returns `Session` instead of `Optional<Session>`.
    In `CaffeineSessionAdapter`:
    - Add a `Cache<UUID, String> tombstoneCache` to store eviction reasons.
    - Set up a removalListener on the main `sessionCache`. If cause is `EXPIRED`, insert "EXPIRED" into `tombstoneCache`.
    - Modify `delete(UUID id)` to manually put "DELETED" into `tombstoneCache` before calling `sessionCache.invalidate(id)`.
    - Modify `load(UUID id)`: if `sessionCache.getIfPresent(id)` is null, check `tombstoneCache`. If "DELETED", throw `SessionDeletedException`. If "EXPIRED", throw `SessionExpiredException`. Otherwise, throw `SessionNotFoundException`.
  </action>
  <verify>
    <automated>grep "tombstoneCache" src/main/java/br/com/saveeditor/brasfoot/adapters/out/cache/CaffeineSessionAdapter.java</automated>
  </verify>
  <done>Adapter throws correct exception based on tombstone cache.</done>
</task>

<task type="auto">
  <name>Delete Session on Download and Map Statuses</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/application/services/SessionService.java, src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java</files>
  <action>
    Update `SessionService.download`: delete the session using `sessionStatePort.delete(id)` *after* writing the payload but before returning it. Also remove the `.orElseThrow` since `load()` now returns `Session` directly.
    Update `GlobalExceptionHandler` to map:
    - `SessionNotFoundException` and `SessionDeletedException` -> 404 Not Found
    - `SessionExpiredException` -> 410 Gone
    Verify everything still compiles.
  </action>
  <verify>
    <automated>mvn clean test</automated>
  </verify>
  <done>Download triggers deletion and API returns appropriate HTTP status codes based on session history.</done>
</task>

</tasks>