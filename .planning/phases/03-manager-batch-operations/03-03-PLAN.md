---
phase: 03-manager-batch-operations
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified: 
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/batch/ManagerBatchItem.java
  - src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/ManagerController.java
autonomous: true
requirements: [EDIT-03, DX-02]

must_haves:
  truths:
    - User can submit an array of manager updates and get a 207 response if some fail.
    - User can view realistic batch update examples in Swagger UI for Managers.
  artifacts:
    - path: src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/ManagerController.java
      provides: POST /batch/managers endpoint
  key_links:
    - from: src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/ManagerController.java
      to: src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java
      via: batchUpdateManagers method call returning BatchResponse
---

<objective>
Introduce batch update operations for the new Manager entities using the established Custom Bulk Objects. Ensure Swagger documentation provides full, realistic example payloads for the manager batch endpoint.

Purpose: Bring manager editing up to parity with teams and players regarding bulk edits (EDIT-03, DX-02).
Output: ManagerBatchItem DTO, batch processing method in Manager service, and typed batch REST endpoint for managers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-manager-batch-operations/03-CONTEXT.md

## Key Decisions
- **D-01:** Custom Bulk Objects (arrays of typed items).
- **D-02:** Typed endpoints (POST `/batch/managers`).
- **D-03:** Hard limit on batch sizes (400 Bad Request, e.g. `brasfoot.api.batch.max-size=1000`).
- **D-05:** Partial Success (207 Multi-Status response).
- **D-06:** Errors map back to request indices.
- **D-09:** Summary logging only.
- **D-10:** Realistic example payloads in Swagger.
</context>

<interfaces>
From `src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java` (Plan 1 Output):
```java
public Manager updateManager(UUID sessionId, int managerId, ...);
```
From `src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/batch/BatchResponse.java` (Plan 2 Output):
```java
public class BatchResponse {
    // totalProcessed, successful, failed, List<BatchItemError> errors
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement Manager Batch Endpoint</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/batch/ManagerBatchItem.java, src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java, src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/ManagerController.java</files>
  <action>
    - Create `ManagerBatchItem` extending or wrapping `ManagerUpdateRequest` with `int managerId` for identity tracking.
    - In `ManagerController`, implement `POST /api/v1/sessions/{sessionId}/batch/managers`. Read `@Value("${brasfoot.api.batch.max-size:1000}")` for limit (D-03).
    - Check if size > limit, reject 400 Bad Request.
    - Document via Swagger using `@Operation` to fulfill D-10 with realistic `List<ManagerBatchItem>` payload.
    - In `ManagerManagementService`, implement `batchUpdateManagers(UUID, List<ManagerBatchItem>)`. 
    - Iterate requests calling existing `updateManager` logic. Catch `Exception`, build `BatchItemError` mapped by index.
    - Implement summary logging (`log.info("Batch managers applied: {} total, {} success, {} failed", ...)`) to fulfill D-09.
    - Controller returns HTTP 207 Multi-Status if `errors` exist, HTTP 200 OK otherwise.
  </action>
  <verify>
    <automated>mvn clean test-compile</automated>
  </verify>
  <done>Manager batch endpoint operates accurately with 207 tracking and validates limits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Denial of Service risk through huge bulk payloads on newly implemented manager endpoint. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service | ManagerController | mitigate | Use the same configured max batch size limit block to short circuit processing before allocating service layer objects. |
</threat_model>

<verification>
- Confirm 207 Multi-Status logic functions properly for Manager iterations.
</verification>

<success_criteria>
- Code compiles.
- Manager batch endpoint is accessible in Swagger and lists a realistic Manager payload.
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-batch-operations/03-03-SUMMARY.md`
</output>