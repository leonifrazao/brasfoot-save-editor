---
phase: 03-manager-batch-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - src/main/java/br/com/saveeditor/brasfoot/util/BrasfootConstants.java
  - src/main/java/br/com/saveeditor/brasfoot/domain/Manager.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/ManagerDto.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/ManagerUpdateRequest.java
  - src/main/java/br/com/saveeditor/brasfoot/application/ports/in/GetManagerUseCase.java
  - src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdateManagerUseCase.java
  - src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/ManagerController.java
autonomous: true
requirements: [EDIT-03]

must_haves:
  truths:
    - User can retrieve a list of all managers in a session.
    - User can retrieve a specific manager by their index/ID.
    - User can update a specific manager's properties (like name, age, reputation) via API.
    - Team assignment for a manager remains strictly read-only and cannot be updated.
  artifacts:
    - path: src/main/java/br/com/saveeditor/brasfoot/domain/Manager.java
      provides: Manager domain model
    - path: src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/ManagerController.java
      provides: REST endpoints for manager editing
      exports: ["GET", "PATCH"]
  key_links:
    - from: src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/ManagerController.java
      to: src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java
      via: constructor injection and endpoint delegation
---

<objective>
Implement core API operations to fetch and update Manager entities within a Brasfoot save session, fully supporting custom attributes like name, age, reputation, and trophies while leaving team assignment read-only.

Purpose: Enable programmatic, fine-grained editing of career profiles (D-07, D-08).
Output: Manager domain model, mapping services, DTOs, and REST controller for single-manager operations.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-manager-batch-operations/03-CONTEXT.md

## Key Decisions
- **D-07:** All manager attributes are editable (Name, Nationality, Age, Reputation, Trophies, etc.)
- **D-08:** Team assignment is read-only (do not expose teamId in `ManagerUpdateRequest`).
</context>

<interfaces>
From `src/main/java/br/com/saveeditor/brasfoot/service/GameDataService.java`:
```java
public List<Object> getManagers(Object root);
public Object getHumanManager(Object root);
```
From `src/main/java/br/com/saveeditor/brasfoot/util/BrasfootConstants.java`:
```java
public static final String HUMAN_MANAGERS_LIST = "al"; // List<Manager>
public static final String MANAGER_NAME = "dm"; // String
public static final String MANAGER_IS_HUMAN = "mW"; // boolean
public static final String MANAGER_CONFIDENCE_BOARD = "of"; // int
public static final String MANAGER_CONFIDENCE_FANS = "og"; // int
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Manager Domain and DTOs</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/util/BrasfootConstants.java, src/main/java/br/com/saveeditor/brasfoot/domain/Manager.java, src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/ManagerDto.java, src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/ManagerUpdateRequest.java</files>
  <action>
    - Add missing Manager constants to `BrasfootConstants.java` as placeholders or guesses (e.g., `MANAGER_AGE`, `MANAGER_NATIONALITY`, `MANAGER_REPUTATION`, `MANAGER_TROPHIES` - map them to logical field names or assume typical single-char keys if known, otherwise use safe fallback defaults like `"age"`, `"nationality"`, `"reputation"` with a comment that they need verification).
    - Create `Manager` domain model containing id (index), name, isHuman, teamId, confidenceBoard, confidenceFans, age, nationality, reputation, and trophies.
    - Create `ManagerDto` reflecting the domain model.
    - Create `ManagerUpdateRequest` including only the editable fields (name, age, nationality, reputation, trophies). Explicitly omit `teamId` per D-08. Ensure fields use boxed types (e.g., `Integer`) to allow partial updates.
  </action>
  <verify>
    <automated>mvn clean test-compile</automated>
  </verify>
  <done>Manager domain and DTO models compile and enforce D-08.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Manager Use Cases and Service</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/application/ports/in/GetManagerUseCase.java, src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdateManagerUseCase.java, src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java</files>
  <action>
    - Define interfaces `GetManagerUseCase` (list all, get by ID) and `UpdateManagerUseCase` (update by ID).
    - Create `ManagerManagementService` implementing both interfaces.
    - Inject `SessionStatePort` and `GameDataService`.
    - Retrieve managers using `gameDataService.getManagers(root)`. Map to domain model using `ReflectionUtils`.
    - For updates, modify the specific index in the manager list, apply changes via `ReflectionUtils`, validate inputs (e.g., age > 15), and persist the session via `sessionStatePort.save(session)`.
  </action>
  <verify>
    <automated>mvn clean test-compile</automated>
  </verify>
  <done>Service layer correctly interacts with in-memory session objects and applies reflective updates.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ManagerController</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/ManagerController.java</files>
  <action>
    - Create `ManagerController` mapped to `/api/v1/sessions/{sessionId}/managers`.
    - Inject `GetManagerUseCase` and `UpdateManagerUseCase`.
    - Implement `GET /` to return `List<ManagerDto>`.
    - Implement `GET /{managerId}` to return a specific `ManagerDto`.
    - Implement `PATCH /{managerId}` accepting `ManagerUpdateRequest` to apply partial updates.
    - Add Swagger `@Tag`, `@Operation`, and `@ApiResponse` annotations to document these endpoints.
  </action>
  <verify>
    <automated>mvn clean test-compile</automated>
  </verify>
  <done>REST controller exposes standard resource paths for managing managers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted update payloads attempting to inject malformed primitive values via reflection. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ManagerController | mitigate | Use strict @Valid annotations and boxed primitives in DTO. Service layer must hardcode validation ranges (e.g., age 15-100) before reflection. |
| T-03-02 | Info Disclosure | Session Boundary | mitigate | Ensure sessionId validation checks bounds and 404s properly before accessing the manager arrays. |
</threat_model>

<verification>
- Check that D-07 is satisfied (custom attributes can be patched).
- Check that D-08 is satisfied (`teamId` is not editable).
</verification>

<success_criteria>
- `/api/v1/sessions/{id}/managers` endpoints compile and are accessible.
- Manager objects can be updated reflectively.
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-batch-operations/03-01-SUMMARY.md`
</output>