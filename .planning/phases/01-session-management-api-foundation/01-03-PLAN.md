---
phase: 01-session-management-api-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified: ["src/main/java/br/com/saveeditor/brasfoot/adapters/out/file/KryoSaveAdapter.java", "src/main/java/br/com/saveeditor/brasfoot/application/services/SessionService.java", "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionController.java", "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionResponse.java"]
autonomous: false
requirements: [SESS-01, SESS-02, DX-01, D-04, D-08, D-09]
must_haves:
  truths:
    - "Users can POST a raw save file and receive a JSON session ID"
    - "System validates and fully parses the save file on upload"
    - "Users can GET a raw binary save file using their session ID"
    - "Swagger UI displays the endpoints and allows testing"
  artifacts:
    - path: "src/main/java/br/com/saveeditor/brasfoot/adapters/out/file/KryoSaveAdapter.java"
      provides: "Delegates byte[] parsing to existing SaveFileService"
    - path: "src/main/java/br/com/saveeditor/brasfoot/application/services/SessionService.java"
      provides: "Application logic bridging Web -> State/File Ports"
    - path: "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionController.java"
      provides: "REST endpoints for /api/v1/sessions"
  key_links:
    - from: "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionController.java"
      to: "/api/v1/sessions"
      via: "Spring @RestController mappings"
      pattern: "@PostMapping"
---

<objective>
Implement the core Use Cases and REST endpoints for session lifecycle, wired via the Hexagonal structure.

Purpose: Completes Phase 1 goals allowing users to upload (SESS-01) and download (SESS-02) saves, with upfront validation (DX-01, D-04). Establishes the `POST` / `GET` REST contracts (D-08, D-09).
Output: SessionService, SessionController, and the KryoSaveAdapter.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-session-management-api-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement File Adapter and Application Service</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/adapters/out/file/KryoSaveAdapter.java, src/main/java/br/com/saveeditor/brasfoot/application/services/SessionService.java</files>
  <action>Create `KryoSaveAdapter` (implements `LoadSavePort`, `WriteSavePort`). Use the existing `SaveFileService` / `Kryo` logic to convert a `byte[]` to `EditorShellContext` and vice-versa, guaranteeing full validation (D-04). Create `SessionService` (implements `UploadSaveUseCase`, `DownloadSaveUseCase`), bridging the `SessionStatePort` and File Ports.</action>
  <verify>
    <automated>grep -q "SessionService" src/main/java/br/com/saveeditor/brasfoot/application/services/SessionService.java</automated>
  </verify>
  <done>Adapter delegates parsing; Service orchestrates the upload/download flows</done>
</task>

<task type="auto">
  <name>Task 2: Implement REST Controller</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionResponse.java, src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionController.java</files>
  <action>Create a `SessionResponse(String sessionId)` record. Implement `SessionController` with `@RestController` at base path `/api/v1/sessions` (D-08). Endpoint `POST /` accepts a `MultipartFile` and returns a JSON `SessionResponse`. Endpoint `GET /{id}/download` returns `application/octet-stream` byte array (D-09).</action>
  <verify>
    <automated>grep -q "@RestController" src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionController.java</automated>
  </verify>
  <done>SessionController exposes the endpoints</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify APIs and Swagger UI</name>
  <action>Developer to manually run application and test APIs via Swagger UI</action>
  <files>none</files>
  <verify>Manually test swagger UI</verify>
  <done>Testing succeeds</done>
  <what-built>Swagger UI and Session API (Phase 1 Goal)</what-built>
  <how-to-verify>
    1. Run `mvn spring-boot:run`
    2. Visit `http://localhost:8080/swagger-ui.html`
    3. Test `POST /api/v1/sessions` with a valid save file and receive an ID
    4. Test `GET /api/v1/sessions/{id}/download` with the ID and confirm binary download
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File Upload | User-supplied `.sav` file entering parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | KryoSaveAdapter | mitigate | Ensure strict parsing failure throws an exception mapped to RFC-7807 (DX-01, D-04, D-05) |
</threat_model>

<verification>
ArchUnit tests and Spring Context tests pass.
</verification>

<success_criteria>
- Developer can view endpoints via Swagger UI (DX-03).
- Valid save uploads return a Session ID JSON (SESS-01).
- System rejects corrupted files with ProblemDetail (DX-01).
- Modified saves download as raw byte streams (SESS-02).
</success_criteria>

<output>
After completion, create `.planning/phases/01-session-management-api-foundation/01-03-SUMMARY.md`
</output>
