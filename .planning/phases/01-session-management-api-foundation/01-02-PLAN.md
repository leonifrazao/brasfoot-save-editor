---
phase: 01-session-management-api-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["pom.xml", "src/main/java/br/com/saveeditor/brasfoot/domain/Session.java", "src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UploadSaveUseCase.java", "src/main/java/br/com/saveeditor/brasfoot/application/ports/in/DownloadSaveUseCase.java", "src/main/java/br/com/saveeditor/brasfoot/application/ports/out/SessionStatePort.java", "src/main/java/br/com/saveeditor/brasfoot/application/ports/out/LoadSavePort.java", "src/main/java/br/com/saveeditor/brasfoot/application/ports/out/WriteSavePort.java", "src/main/java/br/com/saveeditor/brasfoot/adapters/out/cache/CaffeineSessionAdapter.java"]
autonomous: true
requirements: [SESS-01, SESS-02, D-06, D-07]
must_haves:
  truths:
    - "Domain models and UseCases defined for Session lifecycle"
    - "SessionStatePort provides save, load, and delete contracts"
    - "Caffeine Adapter caches sessions with a 1-hour absolute TTL"
  artifacts:
    - path: "src/main/java/br/com/saveeditor/brasfoot/domain/Session.java"
      provides: "Session model representing an active user save edit"
    - path: "src/main/java/br/com/saveeditor/brasfoot/application/ports/out/SessionStatePort.java"
      provides: "In-memory persistence contract"
    - path: "src/main/java/br/com/saveeditor/brasfoot/adapters/out/cache/CaffeineSessionAdapter.java"
      provides: "Caffeine cache implementation of SessionStatePort"
  key_links:
    - from: "src/main/java/br/com/saveeditor/brasfoot/adapters/out/cache/CaffeineSessionAdapter.java"
      to: "com.github.ben-manes.caffeine:caffeine"
      via: "Maven dependency"
      pattern: "Caffeine"
---

<objective>
Define the Domain Models, Ports, and Cache Adapter for Session state management.

Purpose: Creates the core Hexagonal contracts (SESS-01, SESS-02, D-02) and an in-memory caching mechanism (D-06, D-07) to store the uploaded game state without a persistent database.
Output: Session class, Ports, and the Caffeine adapter.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-session-management-api-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Domain Models & Use Cases</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/domain/Session.java, src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UploadSaveUseCase.java, src/main/java/br/com/saveeditor/brasfoot/application/ports/in/DownloadSaveUseCase.java</files>
  <action>Create a `Session` record/class containing `id` (UUID) and `EditorShellContext` (the existing in-memory state). Create interface `UploadSaveUseCase` returning `String` (session id) given a `byte[]` payload. Create interface `DownloadSaveUseCase` returning `byte[]` payload given a session id.</action>
  <verify>
    <automated>grep -q "UploadSaveUseCase" src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UploadSaveUseCase.java</automated>
  </verify>
  <done>Session domain entity and input ports (UseCases) defined</done>
</task>

<task type="auto">
  <name>Task 2: Define Output Ports</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/application/ports/out/SessionStatePort.java, src/main/java/br/com/saveeditor/brasfoot/application/ports/out/LoadSavePort.java, src/main/java/br/com/saveeditor/brasfoot/application/ports/out/WriteSavePort.java</files>
  <action>Create output ports: `SessionStatePort` (save/load/delete Session), `LoadSavePort` (parse `byte[]` -> `EditorShellContext`), and `WriteSavePort` (serialize `EditorShellContext` -> `byte[]`). This establishes boundaries (D-02).</action>
  <verify>
    <automated>grep -q "SessionStatePort" src/main/java/br/com/saveeditor/brasfoot/application/ports/out/SessionStatePort.java</automated>
  </verify>
  <done>Output ports defined for cache and file serialization</done>
</task>

<task type="auto">
  <name>Task 3: Implement Caffeine Cache Adapter</name>
  <files>pom.xml, src/main/java/br/com/saveeditor/brasfoot/adapters/out/cache/CaffeineSessionAdapter.java</files>
  <action>Add `com.github.ben-manes.caffeine:caffeine` dependency to `pom.xml`. Implement `SessionStatePort` via a `@Component` named `CaffeineSessionAdapter`. Configure the cache builder with an absolute TTL (`expireAfterWrite`) of 1 hour (D-06, D-07).</action>
  <verify>
    <automated>mvn dependency:resolve -DincludeArtifactIds=caffeine && grep -q "expireAfterWrite" src/main/java/br/com/saveeditor/brasfoot/adapters/out/cache/CaffeineSessionAdapter.java</automated>
  </verify>
  <done>Caffeine adapter implements the SessionStatePort with correct TTL</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-Memory Cache | Cached sessions containing parsed game files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Denial of Service | CaffeineSessionAdapter | mitigate | Caffeine cache ensures strict TTL limits (1 hour) to prevent memory leak |
</threat_model>

<verification>
`mvn clean test` runs without ArchUnit or compilation failures.
</verification>

<success_criteria>
- Session model exists.
- Ports strictly follow Hexagonal naming (UseCase, Port).
- CaffeineSessionAdapter initializes and caches sessions with a 1-hour write expiration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-session-management-api-foundation/01-02-SUMMARY.md`
</output>
