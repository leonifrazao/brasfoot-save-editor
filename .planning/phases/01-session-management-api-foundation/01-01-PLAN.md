---
phase: 01-session-management-api-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["pom.xml", "src/test/java/br/com/saveeditor/brasfoot/architecture/HexagonalArchitectureTest.java", "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java"]
autonomous: true
requirements: [DX-03, D-01, D-02, D-03, D-05]
must_haves:
  truths:
    - "Build includes Web and OpenAPI dependencies"
    - "ArchUnit tests enforce Hexagonal package structure"
    - "REST API returns RFC-7807 problem details on errors"
  artifacts:
    - path: "pom.xml"
      provides: "REST, OpenAPI, and ArchUnit dependencies"
    - path: "src/test/java/br/com/saveeditor/brasfoot/architecture/HexagonalArchitectureTest.java"
      provides: "Hexagonal boundaries validation"
    - path: "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java"
      provides: "ProblemDetail exception handling"
  key_links:
    - from: "pom.xml"
      to: "springdoc-openapi-starter-webmvc-ui"
      via: "Maven dependencies"
      pattern: "springdoc-openapi"
---

<objective>
Setup the Hexagonal Architecture foundation, enforce boundaries with ArchUnit, and initialize REST / OpenAPI capabilities.

Purpose: Establishes the core structure (D-01, D-02) and guarantees clean architecture via tests (D-03). Adds standard RFC-7807 error handling (D-05) and OpenAPI docs (DX-03) before domain logic is built.
Output: Configured `pom.xml`, ArchUnit test, Hexagonal directories, and GlobalExceptionHandler.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-session-management-api-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dependencies</name>
  <files>pom.xml</files>
  <action>Add `spring-boot-starter-web`, `springdoc-openapi-starter-webmvc-ui` (v2.3.0 or compatible), and test dependencies `spring-boot-starter-test` and `com.tngtech.archunit:archunit-junit5:1.2.1` to the POM. Keep existing dependencies.</action>
  <verify>
    <automated>mvn dependency:resolve -DincludeArtifactIds=spring-boot-starter-web,springdoc-openapi-starter-webmvc-ui,archunit-junit5</automated>
  </verify>
  <done>Dependencies resolve successfully</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Setup Hexagonal Structure & ArchUnit Tests</name>
  <files>src/test/java/br/com/saveeditor/brasfoot/architecture/HexagonalArchitectureTest.java</files>
  <behavior>
    - Test: `domain` package must not depend on `application` or `adapters`.
    - Test: `application` package must not depend on `adapters`.
    - Test: Interfaces in `application.ports.in` must end with `UseCase`.
    - Test: Interfaces in `application.ports.out` must end with `Port`.
  </behavior>
  <action>Create the package structure `domain`, `application.ports.in`, `application.ports.out`, `application.services`, `adapters.in.web`, `adapters.out` under `br.com.saveeditor.brasfoot`. Create the `HexagonalArchitectureTest` using ArchUnit JUnit5 to enforce D-01, D-02, and D-03.</action>
  <verify>
    <automated>mvn test -Dtest=HexagonalArchitectureTest</automated>
  </verify>
  <done>ArchUnit tests pass, enforcing structural rules</done>
</task>

<task type="auto">
  <name>Task 3: Global Exception Handler</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java</files>
  <action>Create a `@ControllerAdvice` in the web adapter package. Add handlers that return Spring's `ProblemDetail` (RFC-7807) for `IllegalArgumentException` and generic `Exception`, ensuring standard API error responses (D-05).</action>
  <verify>
    <automated>grep -q "@ControllerAdvice" src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/GlobalExceptionHandler.java</automated>
  </verify>
  <done>ControllerAdvice returns ProblemDetail for errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Web API | HTTP requests enter application |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | GlobalExceptionHandler | mitigate | Use RFC-7807 ProblemDetail but strip stack traces from response |
</threat_model>

<verification>
`mvn clean test` succeeds.
</verification>

<success_criteria>
- POM contains Web, OpenAPI, and ArchUnit dependencies.
- ArchUnit test exists and passes.
- GlobalExceptionHandler is implemented using ProblemDetail.
</success_criteria>

<output>
After completion, create `.planning/phases/01-session-management-api-foundation/01-01-SUMMARY.md`
</output>
