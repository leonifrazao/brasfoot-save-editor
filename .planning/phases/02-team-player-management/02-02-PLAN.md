---
phase: 02-team-player-management
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/main/java/br/com/saveeditor/brasfoot/domain/Player.java
  - src/main/java/br/com/saveeditor/brasfoot/application/ports/in/GetPlayerUseCase.java
  - src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdatePlayerUseCase.java
  - src/main/java/br/com/saveeditor/brasfoot/application/services/PlayerManagementService.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/PlayerController.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/PlayerDto.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/PlayerUpdateRequest.java
autonomous: true
requirements: [EDIT-02]

must_haves:
  truths:
    - "User can list all players in a team"
    - "User can retrieve a specific player's details"
    - "User can update a player's age, overall, energy, morale, and position"
  artifacts:
    - path: "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/PlayerController.java"
      provides: "REST endpoints for players"
      contains: "@RestController"
    - path: "src/main/java/br/com/saveeditor/brasfoot/application/services/PlayerManagementService.java"
      provides: "Player use cases implementation"
      contains: "implements GetPlayerUseCase, UpdatePlayerUseCase"
  key_links:
    - from: "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/PlayerController.java"
      to: "GetPlayerUseCase"
      via: "constructor injection"
      pattern: "private final GetPlayerUseCase"
---

<objective>
Implement REST API endpoints and application services for viewing and editing Player attributes within a Team.

Purpose: Allow programmatic access to player data (stats, position, age, energy, etc.) inside a loaded session.
Output: PlayerController and PlayerManagementService.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing useful service
@src/main/java/br/com/saveeditor/brasfoot/service/GameDataService.java
@src/main/java/br/com/saveeditor/brasfoot/util/BrasfootConstants.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Player Domain and Ports</name>
  <files>
    src/main/java/br/com/saveeditor/brasfoot/domain/Player.java,
    src/main/java/br/com/saveeditor/brasfoot/application/ports/in/GetPlayerUseCase.java,
    src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdatePlayerUseCase.java,
    src/main/java/br/com/saveeditor/brasfoot/application/services/PlayerManagementService.java
  </files>
  <read_first>
    - src/main/java/br/com/saveeditor/brasfoot/application/services/TeamManagementService.java
    - src/main/java/br/com/saveeditor/brasfoot/service/GameDataService.java
    - src/main/java/br/com/saveeditor/brasfoot/util/BrasfootConstants.java
  </read_first>
  <action>
    Create a `Player` domain record representing player data (index/id, name, age, overall, position, energy, morale, etc.). Note: `index` inside the `TEAM_PLAYERS` list is often the safest unique identifier, so `Player` should include `Integer id` mapped to this index or the underlying `PLAYER_ID` if stable.
    Define `GetPlayerUseCase` with `List<Player> getTeamPlayers(UUID sessionId, int teamId)` and `Player getPlayer(UUID sessionId, int teamId, int playerId)`.
    Define `UpdatePlayerUseCase` with `Player updatePlayer(UUID sessionId, int teamId, int playerId, Integer age, Integer overall, Integer position, Integer energy, Integer morale)`.
    Implement `PlayerManagementService` mapping these inputs to the real objects using `GameDataService`, mutating via `ReflectionUtils` and `BrasfootConstants`, and persisting the changes via `sessionStatePort.save()`.
  </action>
  <verify>
    <automated>mvn clean compile</automated>
  </verify>
  <acceptance_criteria>
    - PlayerManagementService.java contains `implements GetPlayerUseCase, UpdatePlayerUseCase`
    - PlayerManagementService.java retrieves players from the specified team
    - PlayerManagementService.java contains `sessionStatePort.save(session)`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Player Controller</name>
  <files>
    src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/PlayerController.java,
    src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/PlayerDto.java,
    src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/PlayerUpdateRequest.java
  </files>
  <read_first>
    - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/TeamController.java
    - src/main/java/br/com/saveeditor/brasfoot/application/ports/in/GetPlayerUseCase.java
    - src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdatePlayerUseCase.java
  </read_first>
  <action>
    Create `PlayerDto` and `PlayerUpdateRequest` (all fields optional).
    Create `PlayerController` annotated with `@RestController` and mapped to `/api/v1/sessions/{sessionId}/teams/{teamId}/players`.
    Implement `GET /` to return a list of `PlayerDto`.
    Implement `GET /{playerId}` to return a single `PlayerDto`.
    Implement `PATCH /{playerId}` passing updates to `UpdatePlayerUseCase` and returning the updated `PlayerDto`.
    Include OpenAPI `@Operation` annotations and `@Tag` for Swagger UI.
  </action>
  <verify>
    <automated>mvn clean compile</automated>
  </verify>
  <acceptance_criteria>
    - PlayerController.java maps to `teams/{teamId}/players`
    - PlayerController.java contains `@PatchMapping("/{playerId}")`
    - PlayerController.java contains `@Operation(summary =` tags
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Application | Untrusted external HTTP requests mapping to domain updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | PlayerController | mitigate | Validate updates (e.g. age between 15 and 50, overall between 1 and 100) before reflection to avoid game crashes |
</threat_model>

<verification>
Ensure compilation passes and REST endpoints correctly parse request bodies and path variables.
</verification>

<success_criteria>
Endpoints correctly read and update player attributes using the in-memory generic object state, making sure save() is called afterward.
</success_criteria>

<output>
After completion, create `.planning/phases/02-team-player-management/02-02-SUMMARY.md`
</output>
