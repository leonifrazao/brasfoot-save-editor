---
phase: 02-team-player-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/br/com/saveeditor/brasfoot/domain/Team.java
  - src/main/java/br/com/saveeditor/brasfoot/application/ports/in/GetTeamUseCase.java
  - src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdateTeamUseCase.java
  - src/main/java/br/com/saveeditor/brasfoot/application/services/TeamManagementService.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/TeamController.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/TeamDto.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/TeamUpdateRequest.java
autonomous: true
requirements: [EDIT-01]

must_haves:
  truths:
    - "User can list all teams in the session"
    - "User can retrieve a specific team's details"
    - "User can update a team's finances and reputation"
  artifacts:
    - path: "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/TeamController.java"
      provides: "REST endpoints for teams"
      contains: "@RestController"
    - path: "src/main/java/br/com/saveeditor/brasfoot/application/services/TeamManagementService.java"
      provides: "Team use cases implementation"
      contains: "implements GetTeamUseCase, UpdateTeamUseCase"
  key_links:
    - from: "src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/TeamController.java"
      to: "GetTeamUseCase"
      via: "constructor injection"
      pattern: "private final GetTeamUseCase"
---

<objective>
Implement REST API endpoints and application services for viewing and editing Team statistics and finances.

Purpose: Allow programmatic access to team data within a loaded save session.
Output: TeamController and TeamManagementService.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing useful service
@src/main/java/br/com/saveeditor/brasfoot/service/GameDataService.java
@src/main/java/br/com/saveeditor/brasfoot/util/BrasfootConstants.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Team Domain and Ports</name>
  <files>
    src/main/java/br/com/saveeditor/brasfoot/domain/Team.java,
    src/main/java/br/com/saveeditor/brasfoot/application/ports/in/GetTeamUseCase.java,
    src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdateTeamUseCase.java,
    src/main/java/br/com/saveeditor/brasfoot/application/services/TeamManagementService.java
  </files>
  <read_first>
    - src/main/java/br/com/saveeditor/brasfoot/service/GameDataService.java
    - src/main/java/br/com/saveeditor/brasfoot/util/BrasfootConstants.java
  </read_first>
  <action>
    Create a `Team` domain record representing the team data (id, name, money, reputation).
    Define `GetTeamUseCase` with `List<Team> getAllTeams(UUID sessionId)` and `Team getTeam(UUID sessionId, int teamId)`.
    Define `UpdateTeamUseCase` with `Team updateTeam(UUID sessionId, int teamId, Long money, Integer reputation)`.
    Implement `TeamManagementService` that uses `SessionStatePort` to load the session, then uses `GameDataService` and `ReflectionUtils` (with constants from `BrasfootConstants`) to map and update the object graph in memory. Crucially, after updating the object via reflection, `SessionStatePort.save(session)` must be called to persist the state in the cache.
  </action>
  <verify>
    <automated>mvn clean compile</automated>
  </verify>
  <acceptance_criteria>
    - TeamManagementService.java contains `implements GetTeamUseCase, UpdateTeamUseCase`
    - TeamManagementService.java contains `sessionStatePort.save(session)` to persist updates
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Team Controller</name>
  <files>
    src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/TeamController.java,
    src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/TeamDto.java,
    src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/dto/TeamUpdateRequest.java
  </files>
  <read_first>
    - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/SessionController.java
    - src/main/java/br/com/saveeditor/brasfoot/application/ports/in/GetTeamUseCase.java
    - src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdateTeamUseCase.java
  </read_first>
  <action>
    Create `TeamDto` and `TeamUpdateRequest` (with optional Long money, Integer reputation).
    Create `TeamController` annotated with `@RestController` and `@RequestMapping("/api/v1/sessions/{sessionId}/teams")`.
    Implement `GET /` to return a list of `TeamDto`.
    Implement `GET /{teamId}` to return a single `TeamDto`.
    Implement `PATCH /{teamId}` accepting a `TeamUpdateRequest` body, passing it to `UpdateTeamUseCase`, and returning the updated `TeamDto`.
    Add OpenAPI `@Operation` annotations so the endpoints appear beautifully in Swagger UI.
  </action>
  <verify>
    <automated>mvn clean compile</automated>
  </verify>
  <acceptance_criteria>
    - TeamController.java contains `@GetMapping` and `@PatchMapping`
    - TeamController.java contains `@Operation(summary =` tags
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Application | Untrusted external HTTP requests mapping to domain updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | TeamController | mitigate | Validate inputs on TeamUpdateRequest, ensure team money is not negative |
</threat_model>

<verification>
Ensure compilation passes and REST endpoints correctly parse request bodies and path variables.
</verification>

<success_criteria>
Endpoints are fully defined and map to underlying Brasfoot constants via reflection safely.
</success_criteria>

<output>
After completion, create `.planning/phases/02-team-player-management/02-01-SUMMARY.md`
</output>
