---
phase: 05-manager-batch-contract-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/record/out/BatchResponse.java
  - src/main/java/br/com/saveeditor/brasfoot/adapters/in/web/record/out/BatchResult.java
autonomous: true
requirements:
  - EDIT-03
user_setup: []
must_haves:
  truths:
    - "Manager age, nationality, reputation, trophies can be updated via PATCH /managers/{managerId}"
    - "Updated manager fields persist in downloaded save file"
    - "All four fields are validated in domain model before reflection"
  artifacts:
    - path: src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java
      provides: "Complete manager field updates in updateManager() and batchUpdateManagers()"
      min_lines: 200
    - path: src/main/java/br/com/saveeditor/brasfoot/util/BrasfootConstants.java
      provides: "Manager field obfuscated name mappings (MANAGER_AGE, MANAGER_NATIONALITY, MANAGER_REPUTATION, MANAGER_TROPHIES)"
      contains: "MANAGER_AGE, MANAGER_NATIONALITY, MANAGER_REPUTATION"
  key_links:
    - from: "ManagerUpdateRequest"
      to: "ManagerManagementService.updateManager()"
      via: "toDomain() mapping"
      pattern: "age, nationality, reputation, trophies"
    - from: "ManagerManagementService"
      to: "BrasfootConstants"
      via: "ReflectionUtils.setFieldValue()"
      pattern: "MANAGER_AGE, MANAGER_NATIONALITY, MANAGER_REPUTATION, MANAGER_TROPHIES"
---

<objective>
Close EDIT-03 gap: Manager field updates completeness.

Purpose: Manager editing API currently skips age, nationality, reputation, and trophies fields despite them being present in ManagerUpdateRequest and domain model. This breaks the requirement "User can update a specific manager's properties (like name, age, reputation, trophies) via API" (Phase 3 verification truth #3).

Output: ManagerManagementService will set all 5 manager fields (name, age, nationality, reputation, trophies) via reflection when updateManager() or batchUpdateManagers() is called.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/phases/05-manager-batch-contract-closure/05-CONTEXT.md
@.planning/phases/03-manager-batch-operations/03-VERIFICATION.md
@.planning/codebase/STACK.md
</execution_context>

<context>
## Current State

ManagerManagementService.updateManager() (lines 71-107):
- Only sets: name, confidenceBoard, confidenceFans (lines 90-98)
- Skips: age, nationality, reputation, trophies
- Request DTO (ManagerUpdateRequest) has all 5 fields with validation (@Min/@Max on age)
- Domain model (Manager) has all 5 fields with builder

ManagerManagementService.batchUpdateManagers() (lines 110-163):
- Same issue: only sets name, confidenceBoard, confidenceFans (lines 144-151)
- Skips: age, nationality, reputation, trophies

Root cause: Lines 89-98 and 143-151 only handle 3 of 5 fields.

BrasfootConstants (lines 54-57): All field names exist and marked "assumed":
- MANAGER_AGE = "age"
- MANAGER_NATIONALITY = "nationality"
- MANAGER_REPUTATION = "reputation"
- MANAGER_TROPHIES = "trophies"

These are assumed (not verified against game data) but will be used as-is per Phase 4 tech debt deferral.

## Why This Matters

Phase 3 verification (03-VERIFICATION.md line 68-69): "ManagerManagementService.updateManager() only updates name, confidenceBoard, confidenceFans. Missing age, nationality, reputation, trophies."

Milestone audit (v1.0-v1.0-MILESTONE-AUDIT.md line 23): "EDIT-03 unsatisfied — manager editing path incomplete".

Users cannot edit manager profiles beyond name and confidence; API contract is broken.

## Architecture Context

- Manager domain model has all fields with builder and validation
- ManagerUpdateRequest has all fields with @Min/@Max constraints
- ReflectionUtils.setFieldValue() is the mechanism for setting obfuscated fields
- SessionStatePort.save() commits the session after updates
- No new exceptions or error handling needed (use existing IllegalArgumentException pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing manager field reflections to updateManager()</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java</files>
  <action>
In ManagerManagementService.updateManager() (lines 89-98), add field updates for age, nationality, reputation, trophies after the existing 3 fields:

```java
if (updateData.getAge() != null) {
    ReflectionUtils.setFieldValue(managerObj, BrasfootConstants.MANAGER_AGE, updateData.getAge());
}
if (updateData.getNationality() != null) {
    ReflectionUtils.setFieldValue(managerObj, BrasfootConstants.MANAGER_NATIONALITY, updateData.getNationality());
}
if (updateData.getReputation() != null) {
    ReflectionUtils.setFieldValue(managerObj, BrasfootConstants.MANAGER_REPUTATION, updateData.getReputation());
}
if (updateData.getTrophies() != null) {
    ReflectionUtils.setFieldValue(managerObj, BrasfootConstants.MANAGER_TROPHIES, updateData.getTrophies());
}
```

Insert after line 98 (after the confidenceFans block). Wrap all 7 field updates in a single try-catch block to maintain existing error handling pattern.

Rationale: All fields are already validated in domain model (Manager.validate()), request DTO has constraints, and constants exist. Just need to apply the same reflection pattern for all 5 fields.
  </action>
  <verify>
    <automated>grep -A 15 "if (updateData.getName() != null)" src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java | grep -c "getAge\|getNationality\|getReputation\|getTrophies" && echo "All 4 fields found"</automated>
  </verify>
  <done>ManagerManagementService.updateManager() sets all 5 fields (name, age, nationality, reputation, trophies) via ReflectionUtils before sessionStatePort.save()</done>
</task>

<task type="auto">
  <name>Task 2: Add missing manager field reflections to batchUpdateManagers()</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java</files>
  <action>
In ManagerManagementService.batchUpdateManagers() (lines 143-151), expand the try-catch block to include the same 4 missing fields after line 151:

```java
if (command.age() != null) {
    ReflectionUtils.setFieldValue(managerObj, BrasfootConstants.MANAGER_AGE, command.age());
}
if (command.nationality() != null) {
    ReflectionUtils.setFieldValue(managerObj, BrasfootConstants.MANAGER_NATIONALITY, command.nationality());
}
if (command.reputation() != null) {
    ReflectionUtils.setFieldValue(managerObj, BrasfootConstants.MANAGER_REPUTATION, command.reputation());
}
if (command.trophies() != null) {
    ReflectionUtils.setFieldValue(managerObj, BrasfootConstants.MANAGER_TROPHIES, command.trophies());
}
```

Insert after line 151 (after the confidenceFans block). Keep within the same try-catch block that wraps all 7 field updates.

Rationale: Batch command is created from ManagerBatchUpdateCommand record which has all fields. Manager validation happens at line 141. Same pattern as updateManager().
  </action>
  <verify>
    <automated>grep -A 20 "if (command.name() != null)" src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java | grep -c "command.age\(\)\|command.nationality\(\)\|command.reputation\(\)\|command.trophies\(\)" && echo "All 4 fields found"</automated>
  </verify>
  <done>ManagerManagementService.batchUpdateManagers() sets all 5 fields (name, age, nationality, reputation, trophies) via ReflectionUtils before sessionStatePort.save()</done>
</task>

<task type="auto">
  <name>Task 3: Run test suite to verify manager updates don't break existing functionality</name>
  <files>src/test/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementServiceTest.java</files>
  <action>
Run the existing ManagerManagementServiceTest to ensure the new field reflections don't break the test compilation or existing test cases:

```bash
mvn test -Dtest=ManagerManagementServiceTest -X
```

If tests fail:
1. Check that all BrasfootConstants.MANAGER_* constants are imported
2. Verify that no new exceptions are thrown that tests don't expect
3. If a test mocks reflection, ensure the mock allows all 5 fields

If no tests exist for the new fields, that's expected (tests were written before fields were complete).
  </action>
  <verify>
    <automated>mvn test -Dtest=ManagerManagementServiceTest 2>&1 | grep -E "(BUILD SUCCESS|BUILD FAILURE)" | tail -1</automated>
  </verify>
  <done>ManagerManagementServiceTest compiles and existing tests pass or are skipped if fixtures don't support new fields</done>
</task>

<task type="auto">
  <name>Task 4: Verify manager field mappings don't cause reflection errors</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/util/ReflectionUtils.java</files>
  <action>
Review ReflectionUtils.setFieldValue() to ensure it handles the case where obfuscated field names in BrasfootConstants might not exist in the actual game object (which is expected for "assumed" field names).

The method should:
1. Attempt to set the field
2. If field not found, log a warn (as it already does in ManagerManagementService)
3. Not throw an exception that would crash the update

Check that the try-catch at ManagerManagementService line 143 (or after Task 1/2) catches exceptions from ReflectionUtils and logs them without propagating.

Rationale: BrasfootConstants marks age, nationality, reputation, trophies as "assumed" (not verified against real game data). If the field name is wrong, reflection will fail silently (already handled). This is acceptable per Phase 4 tech debt deferral.
  </action>
  <verify>
    <automated>grep -A 5 "catch (Exception e)" src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java | grep -c "log.warn\|throw new RuntimeException" && echo "Exception handling found"</automated>
  </verify>
  <done>ReflectionUtils exception handling is reviewed and in place; no changes needed to ReflectionUtils itself</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted input: age (15-100), nationality (string), reputation/trophies (integers) |
| API → Reflection | Trusted: field names from BrasfootConstants (code-owned), values validated in domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | age field via PATCH | mitigate | @Min/@Max constraint in ManagerUpdateRequest validates 15-100 range before Manager.validate() and reflection |
| T-05-02 | Information | nationality field in response | mitigate | No PII in nationality; public game data only |
| T-05-03 | Denial | Invalid field name in BrasfootConstants | accept | Reflection exception caught and logged; update fails silently per existing pattern |

</threat_model>

<verification>
## Automated Tests

1. mvn test -Dtest=ManagerManagementServiceTest passes with new field updates
2. Compile check: All BrasfootConstants.MANAGER_* fields are accessible
3. Manual integration test (if running phase execution):
   - POST /api/v1/sessions/{sessionId}/managers
   - PATCH /api/v1/sessions/{sessionId}/managers/{managerId} with age, nationality, reputation, trophies in request body
   - Verify 200 response with updated fields in ManagerDto
   - Download session and verify fields persisted in save file

## Checklist

- [ ] ManagerManagementService.updateManager() reflects all 5 fields
- [ ] ManagerManagementService.batchUpdateManagers() reflects all 5 fields
- [ ] ManagerManagementServiceTest compiles and passes
- [ ] No new exceptions introduced
- [ ] Domain model validation still runs before reflection

</verification>

<success_criteria>
1. **EDIT-03 truth #3 verified:** User can update a specific manager's properties (name, age, reputation, trophies) via PATCH /api/v1/sessions/{id}/managers/{managerId}
2. **All 5 fields update successfully:** age, nationality, reputation, trophies set via ReflectionUtils alongside name
3. **Persistence confirmed:** Downloaded save file contains updated manager field values
4. **Validation in place:** Invalid age (<15 or >100) rejected with 400 Bad Request
5. **No regression:** Existing manager GET/PATCH/batch tests still pass

</success_criteria>

<output>
After completion, create `.planning/phases/05-manager-batch-contract-closure/05-01-SUMMARY.md` with:
- Requirement fulfilled: EDIT-03 (manager field updates)
- Files modified: ManagerManagementService.java
- Test status: Passed
</output>
