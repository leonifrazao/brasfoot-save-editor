---
phase: 04-add-lombok-and-create-real-domains-with-self-validation-fix-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/test/java/br/com/saveeditor/brasfoot/architecture/HexagonalArchitectureTest.java
  - src/main/java/br/com/saveeditor/brasfoot/application/ports/out/GameDataPort.java
  - src/main/java/br/com/saveeditor/brasfoot/application/ports/in/BatchUpdateManagerUseCase.java
autonomous: true
requirements: [PH4-01, PH4-03]

must_haves:
  truths:
    - "Build supports Lombok-generated accessors/constructors without IDE-only configuration."
    - "Architecture tests fail when application layer depends on concrete data service classes."
    - "Manager batch operations have a first-class use-case interface contract."
  artifacts:
    - path: "pom.xml"
      provides: "Lombok dependency and annotation processor configuration"
      contains: "org.projectlombok:lombok"
    - path: "src/main/java/br/com/saveeditor/brasfoot/application/ports/out/GameDataPort.java"
      provides: "Port contract for reading game entities"
      exports: ["getTeams", "getTeamById", "getPlayers", "getManagers"]
    - path: "src/main/java/br/com/saveeditor/brasfoot/application/ports/in/BatchUpdateManagerUseCase.java"
      provides: "Input use-case contract for manager batch updates"
      exports: ["batchUpdateManagers"]
  key_links:
    - from: "src/test/java/br/com/saveeditor/brasfoot/architecture/HexagonalArchitectureTest.java"
      to: "src/main/java/br/com/saveeditor/brasfoot/application/ports/out/GameDataPort.java"
      via: "ArchUnit rule allowing only ports in application dependencies"
      pattern: "application_must_not_depend_on_service_package"
---

<objective>
Establish Lombok and interface-first contracts so Phase 4 refactors happen against explicit, enforceable boundaries.

Purpose: Reduce refactor risk by locking compilation/architecture constraints before domain and package moves.
Output: Lombok-enabled build, new port/use-case interfaces, and stricter ArchUnit guardrails.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-add-lombok-and-create-real-domains-with-self-validation-fix-/04-RESEARCH.md
@src/main/java/br/com/saveeditor/brasfoot/application/services/TeamManagementService.java
@src/main/java/br/com/saveeditor/brasfoot/application/services/PlayerManagementService.java
@src/main/java/br/com/saveeditor/brasfoot/application/services/ManagerManagementService.java
</context>

<interfaces>
From `src/main/java/br/com/saveeditor/brasfoot/service/GameDataService.java`:
```java
public List<Object> getTeams(Object root);
public Object getTeamById(Object root, int id);
public List<Object> getPlayers(Object team);
public List<Object> getManagers(Object root);
```

From `src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdateManagerUseCase.java`:
```java
Manager updateManager(String sessionId, int managerId, Manager updateData);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Enable Lombok in Maven build</name>
  <files>pom.xml</files>
  <read_first>pom.xml</read_first>
  <action>Add `org.projectlombok:lombok` with `provided` scope in dependencies, then add `maven-compiler-plugin` (under build/plugins) configured with `<annotationProcessorPaths>` containing Lombok so CI and CLI builds generate Lombok code without IDE assumptions.</action>
  <acceptance_criteria>`pom.xml` contains `org.projectlombok` and `maven-compiler-plugin` section with `annotationProcessorPaths` entry for Lombok.</acceptance_criteria>
  <verify>
    <automated>mvn -q -DskipTests compile</automated>
  </verify>
  <done>Lombok annotations are build-supported in local and CI compilation.</done>
</task>

<task type="auto">
  <name>Task 2: Add interface-first contracts for service consistency</name>
  <files>src/main/java/br/com/saveeditor/brasfoot/application/ports/out/GameDataPort.java, src/main/java/br/com/saveeditor/brasfoot/application/ports/in/BatchUpdateManagerUseCase.java</files>
  <read_first>src/main/java/br/com/saveeditor/brasfoot/service/GameDataService.java, src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdateManagerUseCase.java, src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdateTeamUseCase.java, src/main/java/br/com/saveeditor/brasfoot/application/ports/in/UpdatePlayerUseCase.java</read_first>
  <action>Create `GameDataPort` under `application/ports/out` mirroring currently used read operations (`getTeams`, `getTeamById`, `getPlayers`, `getManagers`). Create `BatchUpdateManagerUseCase` under `application/ports/in` with method signature `List&lt;Manager&gt; batchUpdateManagers(String sessionId, List&lt;ManagerBatchUpdateCommand&gt; commands);` and add a new `ManagerBatchUpdateCommand` record in the same package if needed for compile-complete contract parity with Team/Player batch use cases.</action>
  <acceptance_criteria>`GameDataPort.java` exists with the four read methods; `BatchUpdateManagerUseCase.java` exists and declares `batchUpdateManagers` with explicit typed parameters.</acceptance_criteria>
  <verify>
    <automated>mvn -q -DskipTests compile</automated>
  </verify>
  <done>Service/use-case consistency contracts exist before implementation rewiring.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten architecture guardrails for Phase 4</name>
  <files>src/test/java/br/com/saveeditor/brasfoot/architecture/HexagonalArchitectureTest.java</files>
  <read_first>src/test/java/br/com/saveeditor/brasfoot/architecture/HexagonalArchitectureTest.java, src/main/java/br/com/saveeditor/brasfoot/application/services/TeamManagementService.java, src/main/java/br/com/saveeditor/brasfoot/domain/SaveContext.java</read_first>
  <action>Extend ArchUnit tests with rules that (1) forbid classes in `..application..` from depending on `..service..` concrete package (except adapters), and (2) forbid `..domain..` from depending on `..model..` package so future domain self-validation is isolated from transport/state artifacts.</action>
  <acceptance_criteria>`HexagonalArchitectureTest.java` contains rule names `application_must_not_depend_on_service_package` and `domain_must_not_depend_on_model_package`; running the test class passes.</acceptance_criteria>
  <verify>
    <automated>mvn -q -Dtest=HexagonalArchitectureTest test</automated>
  </verify>
  <done>Architecture tests codify interface-first and domain-boundary constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build toolchain → compiled bytecode | Annotation processing changes generated methods and can silently alter behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | pom.xml compiler setup | mitigate | Pin Lombok in dependency + annotation processor path so generated code is deterministic across environments. |
| T-04-02 | E | application/services package | mitigate | Enforce ArchUnit restriction against concrete `service` dependencies to prevent uncontrolled privilege of infrastructure classes. |
</threat_model>

<verification>
Run `mvn -q -Dtest=HexagonalArchitectureTest test` and confirm new rules exist and pass.
</verification>

<success_criteria>
- Lombok is configured for Maven compile pipeline.
- New GameData and Manager batch contracts exist.
- Architecture tests now enforce domain/application boundary tightening.
</success_criteria>

<output>
After completion, create `.planning/phases/04-add-lombok-and-create-real-domains-with-self-validation-fix-/04-01-SUMMARY.md`
</output>
